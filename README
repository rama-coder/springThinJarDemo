Objective here is to download thin jar and use it. Thanks to Dave. Adding missing plugin config and switching phase from verify to default fixed the issue.

Following no longer throws "No deployables found" error.

mvn -X package org.springframework.boot.experimental:spring-boot-thin-maven-plugin:resolve
